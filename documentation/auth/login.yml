tags:
  - "auth"
parameters:
  - in: "body"
    name: "body"
    required: true
    schema:
      $ref: "#/definitions/LoginRequest"
responses:
  200:
    schema:
      $ref: "#/definitions/TokenPair"
