tags:
  - "users"
security:
  - token: []
parameters:
  - in: "body"
    name: "body"
    required: true
    schema:
      $ref: "#/definitions/ResendUserConfirmationRequest"
responses:
  200:
    schema:
      $ref: "#/definitions/User"
