tags:
  - "users"
security:
  - token: []
parameters:
  - in: "body"
    name: "body"
    required: true
    schema:
      $ref: "#/definitions/AdminRequest"
responses:
  200:
    schema:
      $ref: "#/definitions/User"
