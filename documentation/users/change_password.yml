tags:
  - "users"
security:
  - token: []
parameters:
  - in: "body"
    name: "body"
    required: true
    schema:
      $ref: "#/definitions/ChangePasswordRequest"
responses:
  200:
    schema:
      $ref: "#/definitions/TokenPair"
