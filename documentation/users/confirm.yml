tags:
  - "users"
security:
  - token: []
parameters:
  - in: "body"
    name: "body"
    required: true
    schema:
      $ref: "#/definitions/UserConfirmationRequest"
responses:
  200:
    schema:
      $ref: "#/definitions/User"
